echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.bmp"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.JPEG"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.JFIF"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.Exif"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.TIFF"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.GIF"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.BMP"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.PNG"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.PPM"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.PGM"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.PBM"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.PNM"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.WebP"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.HEIF"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.BPG"
echo and my head kinda look like this:
echo (base 64 code, too long to paste here) >>"%cd%/myface.PSD"




